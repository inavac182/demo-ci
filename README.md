# demo-ci
	- Endpoints:
		/messages/hello/<name>
			<name> : string needed by the api

			Response: status 200, JSON { message: String}
		/messages/bye/<name>
			<name> : string needed by the api

			Response: status 200, JSON { message: String}
